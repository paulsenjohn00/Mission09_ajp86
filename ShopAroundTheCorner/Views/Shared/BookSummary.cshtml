@{
}

@model Books

<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>
            @Model.Title
            <span class="badge badge-pill badge-primary" style="float:right">
                <small>$@Model.Price</small>
            </span>
        </h4>
    </div>
    <div>
        <h6>Author: @Model.Author</h6>
        <h6>Publisher: @Model.Publisher</h6>
        <h6>ISBN: @Model.Isbn</h6>
        <h6>@Model.PageCount pages</h6>
    </div>
    <div class="card-text p-1">@Model.Category</div>

    <form method="post" asp-page="/Cart">

        <input type="hidden" asp-for="@Model.BookId" />
        <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />

        <span class="card-text">
            <button type="submit" class="btn btn-success btn-sm float-right">Add to Cart</button>
        </span>

    </form>

</div>